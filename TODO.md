# TODO for Fixing ToDo Component

- [x] Fix input onChange handler: change "onchange" to "onChange", "setDate" to "setData", "e.targe.value" to "e.target.value"
- [x] Initialize data state as empty string instead of undefined
- [x] Modify addtodo to check if data is not empty before adding
- [x] Fix deletetodo: correct the filter logic, remove the "-", and fix the callback
- [x] Add key prop to the li elements in the map
